import static org.junit.Assert.assertEquals;

import org.junit.*;
import org.junit.Assert.*;


public class SkillDemoTest {
    public void MultiplyTest(){
        assertEquals(6, SkillDemo.multiply(2, 3)); //a test that passes
    }
}
